# ============================================================
# 1688 登录 Cookie 示例文件（Netscape / curl 格式）
# ============================================================
# 用途：让 scrape_1688.py 以「你已登录的 1688 账号」身份去抓真实榜单。
#       没有它，脚本不会抓取（generate.py 走规则兜底）。
#
# 如何导出（任选其一）：
#   方法 A（推荐，EditThisCookie 插件）：
#     1. 电脑浏览器登录 1688.com（必须是中国 IP、已登录状态）
#     2. 安装插件「EditThisCookie」（Chrome/Edge 应用商店）
#     3. 打开 1688 任意页面 → 点插件图标 → 点「导出」→ 选「Netscape 格式」
#     4. 把内容保存为 cookies_1688.txt（删掉本示例注释行，只留 cookie 数据）
#   方法 B（手动复制）：
#     1. F12 → Application → Cookies → 找到 1688.com 下的 cookie
#     2. 把每条写成一行： 域名\tFALSE\t/\tFALSE\t过期时间戳\t名字\t值
#        例如：  .1688.com\tFALSE\t/\tFALSE\t1893456000\tcna\tAbCdEf==
#     也可用环境变量：导出成 "名字=值; 名字=值" 形式，运行时设 A1688_COOKIE=...
#
# 注意：
#   - Cookie 含登录凭证，请勿提交到公开仓库（已建议加入 .gitignore）。
#   - Cookie 会过期，失效后重新导出即可。
#   - 仅在本机（中国 IP）运行才稳；云主机 IP 易被 1688 拦截。
# ============================================================

# 下面是占位示例（删除并与你的真实 cookie 替换）：
# .1688.com	FALSE	/	FALSE	1893456000	cna	YOUR_CNA_VALUE
# .1688.com	FALSE	/	FALSE	1893456000	__cn_logon__	YOUR_LOGIN_VALUE
